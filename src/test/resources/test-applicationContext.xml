<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <bean id="contextLoaderListener" class="org.springframework.web.context.ContextLoaderListener"/>

    <!-- Импортируем основной конфиг приложения -->
    <import resource="file:src/main/webapp/WEB-INF/applicationContext.xml"/>

    <!-- Включаем сканирование компонентов (если используется) -->
    <context:component-scan base-package="mil.teng254.legacy"/>

    <!-- Mock для ServletContext -->
    <bean id="servletContext" class="org.springframework.mock.web.MockServletContext">
        <constructor-arg value=""/>
    </bean>

    <!-- Mock для HttpServletRequest -->
    <bean id="httpServletRequest" class="org.springframework.mock.web.MockHttpServletRequest" scope="prototype">
        <property name="servletContext" ref="servletContext"/>
    </bean>

    <!-- Mock для HttpServletResponse -->
    <bean id="httpServletResponse" class="org.springframework.mock.web.MockHttpServletResponse" scope="prototype"/>

    <!-- Конфигурация для Thymeleaf (если используется) -->
    <bean id="templateResolver" class="org.thymeleaf.templateresolver.ServletContextTemplateResolver">
        <property name="prefix" value="/WEB-INF/templates/"/>
        <property name="suffix" value=".html"/>
        <property name="templateMode" value="HTML5"/>
        <property name="cacheable" value="false"/>
        <property name="characterEncoding" value="UTF-8"/>
    </bean>

    <bean id="templateEngine" class="org.thymeleaf.spring5.SpringTemplateEngine">
        <property name="templateResolver" ref="templateResolver"/>
    </bean>

    <bean id="thymeleafViewResolver" class="org.thymeleaf.spring5.view.ThymeleafViewResolver">
        <property name="templateEngine" ref="templateEngine"/>
        <property name="characterEncoding" value="UTF-8"/>
    </bean>

</beans>